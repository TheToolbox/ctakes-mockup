{
  "name": "ctakes",
  "version": "1.0.0",
  "description": "This will likely be broken up and placed into more appropriate directories for organiztion, but for now keeping these files together is just easier",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "MIT"
  "dependencies": {
    "socket.io": "^2.0.3"
  }
}
